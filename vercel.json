{
    "version": 2,
    "buildCommand": "npm install",
    "outputDirectory": ".",
    "functions": {
        "api/index.js": {
            "includeFiles": "views/**"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api"
        }
    ]
}